# Credentials — store these as GitHub Secrets, NEVER commit
client_secrets.json
youtube_token.json
.env

# Old notes
PRD.md
setup.sh
# NOTE: auth_youtube.py is intentionally TRACKED — it defines the OAuth
# scopes and is a required setup step (see BLUEPRINT/REPLICATE.md). It
# contains no secrets; those live in client_secrets.json / youtube_token.json,
# both ignored above.

# Generated content
output/
temp/
logs/

# Analyzer timestamped reports (keep state files, not big logs)
analyzer/report_*.json
analyzer/trends.json
analyzer/competitors.json

# Python
__pycache__/
*.pyc
.env

# IDE
.vscode/
.idea/

# Claude Code session data
.claude/

# Test files
test_*.py
test_*.mp3
test_*.wav
*.mp3
*.wav

# Background video clips — fetched from GitHub Release at runtime
# (kept out of repo to avoid LFS bandwidth quota)
assets/*.mp4
assets/*.mov
