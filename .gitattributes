# LFS tracking removed — background videos are now distributed via GitHub Releases
# (see .github/workflows/create_video.yml for the download step).
# Keeping this file so contributors don't accidentally re-add LFS patterns.
